Steps to run front end app 

Open command prompt(preferably not the VS Code Terminal as it may give errors) and type in the following command
command 1: npx react-native start

Open Android Studion and an emulator then type the command below in a new command prompt
command 2: npx react-native run-android

For a more detailed setup guid go to: https://reactnative.dev/docs/environment-setup